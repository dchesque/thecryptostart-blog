# Contentful API Keys (server-side only — never use NEXT_PUBLIC_ prefix for tokens)
CONTENTFUL_SPACE_ID=your_space_id
CONTENTFUL_ACCESS_TOKEN=your_access_token

# App URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=your_auth_secret

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# Optional: Google Analytics 4
NEXT_PUBLIC_GA4_ID=

# Optional: Google Search Console verification
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Optional: Google AdSense
NEXT_PUBLIC_ADSENSE_CLIENT_ID=

# Seed passwords (required only when running: npx prisma db seed)
# Minimum 12 characters each
# SEED_ADMIN_EMAIL=admin@thecryptostart.com
# SEED_ADMIN_PASSWORD=
# SEED_EDITOR_EMAIL=editor@thecryptostart.com
# SEED_EDITOR_PASSWORD=
# SEED_AUTHOR_EMAIL=author@thecryptostart.com
# SEED_AUTHOR_PASSWORD=
